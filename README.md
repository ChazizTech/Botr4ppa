# Botr4ppaJS

To make a command start the file with this

```js
module.exports = {
    name: 'commandname',
    description: 'Command description'
    execute(msg, args) {
        // Code goes here for command
    },
}
```
